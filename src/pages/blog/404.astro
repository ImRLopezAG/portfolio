---
import NotFound from '@components/pages/404.astro';

const slug = Astro.url.searchParams.get('slug');
---

<NotFound
  desc={`
  The page you are looking for, "${slug}", does not exist. Please check the URL or return to the homepage.
`}
/>
